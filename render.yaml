services:
  - type: web
    name: orvms-maritime-awareness
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: LLM_PROVIDER
        value: fallback
      - key: AIS_PROVIDER
        value: demo
